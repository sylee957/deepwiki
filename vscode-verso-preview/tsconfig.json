{
  // Solution file: the extension is two TypeScript projects with different
  // runtime environments (Node host vs. browser webview). Each has its own
  // tsconfig so the right globals/types load per file. `tsc -b` (see the
  // typecheck script) builds both; the editor routes each file to the project
  // whose `include` matches it.
  "files": [],
  "references": [
    { "path": "./tsconfig.host.json" },
    { "path": "./tsconfig.webview.json" }
  ]
}
